/** New names a user may claim over the lifetime of the account, before grants. */
export const DEFAULT_NEW_NAME_ALLOWANCE = 2

/** Upper bound for a single support grant, guarding against a fat-fingered amount. */
export const MAX_GRANT_PER_CALL = 5
